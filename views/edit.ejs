<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class=" w-full min-h-screen bg-zinc-900 text-white p-10 ">

        <h5 class="mb-5">Edit Your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea name="content" placeholder="What's On Your Mind?"
                class=" block outline-none p-3 w-1/3 resize-none bg-zinc-800 border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>

            <input type="submit" class=" mt-2 w-40 text-sm rounded-md py-2 bg-yellow-500 text-black" value="Update Post">
        </form>

    </div>
    </body-800>

</html>