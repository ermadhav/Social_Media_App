<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class=" w-full min-h-screen bg-zinc-900 text-white p-10 ">
        <div class="w-full flex justify-end">
            <a href="/logout" class=" bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block ">Logout</a>
        </div>

        <div class="flex items-start gap-3">
            <div class="w-10 h-10 rounded-md">
                <img class="w-full h-full overflow-hidden object-cover" src="/images/uploads/<%= user.profilepic %>" alt="Profile Picture">
            </div>
            <h3 class=" text-3xl"><span class="font-light">Hello</span>
                <%= user.name %>
            </h3>
        </div>


        <h5 class="mb-5 mt-3">You can write a post</h5>
        <form method="post" action="/post">
            <textarea name="content" placeholder="What's On Your Mind?"
                class=" block outline-none p-3 w-1/3 resize-none bg-zinc-800 border-2 border-zinc-800 rounded-md"></textarea>

            <input type="submit" class=" mt-2 w-40 text-sm rounded-md py-2 bg-blue-500" value="Create New Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post w-1/3 p-4 mb-5 rounded-md bg-zinc-800 border-zinc-700 border-[1px] ">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="mt-2 inline-block">
                            <%= post.likes.length %> Likes
                        </small>
                        <div class="btns flex gap-4 mt-5">
                            <a href="/like/<%= post._id %>" class="text-blue-500">

                                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>

                            </a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-600">Edit</a>
                        </div>

                    </div>
                    <% }) %>
            </div>
        </div>


    </div>
    </body-800>

</html>